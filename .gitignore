# OS
.DS_Store

# editor
*.pem

# generated caches (managed locally / by GitHub Actions cache, not committed)
/crawl/crawl_cache.json
/crawl/fill_cache.json

# Pages deploy artifact (assembled by CI)
/site/

# downloaded source html (if re-fetched locally)
/komeri.html